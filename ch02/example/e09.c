#include <stdio.h>
int main(int argc, char const *argv[]) {
    int a, b;
    puts("求两个整数的平均值");
    printf("整数a:" );
    scanf("%d", &a);
    printf("整数b:" );
    scanf("%d", &b);
    printf("平均值为：%d\n", (a + b) / 2.0;


    return 0;
}